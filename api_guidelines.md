# 返回格式

**返回格式**：响应包含code、data、msg，成功码为`200`，未登录为`401`,系统错误统一为`500`,`msg`字段应返回友好的提示信息，以便用户快速获取到有效的信息，

    {
          "code": "200",
          "msg": "请求成功",
          "data": {},
          "success": true
     }

**禁止**：返回系统异常栈信息